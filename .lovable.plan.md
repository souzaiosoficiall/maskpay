# Plan - Branding and Text Consolidation for MaskPay

Complete the visual and text identity transition to "MaskPay |" across all remaining routes and components, ensuring a consistent high-end fintech aesthetic.

## User Review Required

> [!IMPORTANT]
> I have already applied the primary branding changes ("MaskPay |") to the main routes. This plan covers the remaining cleanup and minor text fixes found during the audit.

## Proposed Changes

### Visual and Branding
- Update remaining instances of the logo text to include the pipe separator `|` where missing.
- Ensure all meta titles in `__root.tsx` and leaf routes reflect the new branding.
- Standardize the "MaskPay" casing across all informational text.

### Text Edits
- Fix the CTA at the bottom of the landing page from "VENHA SER MASK" to "VENHA SER MASKPAY".
- Add correct Portuguese accents (acentuação) to technical messages where found (e.g., "PROXIMO" -> "PRÓXIMO").
- Update the mock transaction reference in `src/routes/_authenticated.deposit.tsx` to include the correct branding.

### UI Consistency
- Review integration logos in `api-keys.tsx` to ensure they align with the dark, minimalist theme.
- Audit all dashboard sub-routes for residual static placeholders.

## Technical Details

### Branding Logic
- Use literal text `MaskPay |` instead of elided spans to ensure consistent rendering across different font weights.
- Centralize branding strings in `src/routes/__root.tsx` metadata.

### Accentuations
- Manual audit of `src/routes/index.tsx` for uppercase Portuguese text missing accents (e.g. `VOCE` -> `VOCÊ`).

### Integrations
- Verify asset paths for `utmify-logo.png` and `google-analytics-logo.png` to ensure they are being served correctly from the new asset pipeline.
